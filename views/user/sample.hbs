else if(response.notInLimit){
				console.log(response.couponNotExpired.minSpend+ 'hekki' +response.couponNotExpired.maxSpend + 'hekki' +response.total)
				let moreAmount = response.couponNotExpired.minSpend-response.total
				document.getElementById('coupon-error').innerHTML = 'You need to purchase for RS.'+moreAmount+' more to redeem this coupon'
			}

			else if(response.inLimit){
				console.log(response.limitIn.couponOffer,'bucky',response.limitIn.maxSpend,'bucky',response.total)
				let discount = response.total * response.limitIn.couponOffer/100
				let finalTotal = 0
				if(discount>response.limitIn.maxSpend){
					discount = response.limitIn.maxSpend
					 finalTotal = response.total - discount
					 document.getElementById('coupon-error').innerHTML = 'Coupon of '+response.limitIn.couponOffer+' applied upto Rs.'+response.limitIn.maxSpend+' Total Amount to be paid Rs.'+finalTotal

				}else{
					finalTotal = response.total - discount
					document.getElementById('coupon-error').innerHTML = 'Coupon of '+response.limitIn.couponOffer+' applied upto Rs.'+response.limitIn.maxSpend+' Total Amount to be paid Rs.'+finalTotal

				}
			}